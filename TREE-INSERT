mod TREE-INSERT is protecting TREE .
	op insert : Int Tree -> Tree .
	vars I J : Int . vars L R : Tree .
	
	eq insert(I, L J R) = 
			if I <= J then 
				insert(I, L) 
			else 
				insert(I, R) 
			fi .
			
	eq insert(I, empty) = empty I empty [owise] .
endm
